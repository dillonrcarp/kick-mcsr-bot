node_modules
.env
.DS_Store
dist
.env.dillonrcarp
.devcontainer
data/health.json
data/tokens.json
# Runtime user data — accrues real Kick->Minecraft username mappings; must never
# be committed (this repo is public). channels.json stays tracked as seed config.
data/links.json
