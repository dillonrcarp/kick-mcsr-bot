services:
  kickmcsr:
    image: kickmcsr-bot:latest    # use your existing built image
    container_name: kickmcsr
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
